[profile.default]
src = "src"
test = "test"
out = "out"
libs = ["lib"]
optimizer = true
optimizer_runs = 200
via_ir = false
solc_version = "0.8.24"

[profile.default.fuzz]
runs = 1000

[profile.ci.fuzz]
runs = 10000
